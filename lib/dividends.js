Dividends = new Meteor.Collection('dividends');

// _.extend(Dividends.prototype, {
//   refresh: function(symbols, callback) {
    
//   }
// });

if (Meteor.isClient) {
  
}

if (Meteor.isServer) {
  // Meteor.methods({
  //   refresh: function(symbols)
  // });
}